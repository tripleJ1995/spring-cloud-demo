eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

server:
  port: 8201

spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8200
      profile: dev
      label: master


